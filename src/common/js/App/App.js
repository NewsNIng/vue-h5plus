
class App{
    constructor(){

    }

    /**
     * 检查是否已经登录
     * @param {boolean} open 未登录时是否打开登录窗口 默认为false
     * @returns {object} info 用户信息
     */
    checkLogin(open = false){
        let info = {}
        return info
    }
}